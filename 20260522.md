# Aula 1 — Fundamentos do jQuery e primeiro AJAX

> Hoje você vai sair sabendo: o que é jQuery, como selecionar e mexer em qualquer coisa da página, e como pedir dados ao servidor sem recarregar.
> Pasta de trabalho: `lab21/`

---

## Onde salvar seus arquivos

Você tem uma pasta pessoal no servidor do curso: `https://curso.chr.eti.br/SEUNOME/`. Crie `lab21/` dentro dela e salve seus arquivos. Por exemplo, se você for **joão**, sua primeira atividade ficará em:

`https://curso.chr.eti.br/joão/lab21/ativ01-cartao.html`

Abra direto pelo navegador por essa URL. O professor acompanha suas entregas pela mesma URL.

**Versão de referência do professor:** `https://curso.chr.eti.br/professor/lab21/` — abra qualquer arquivo lá e compare com o seu.

---

## Regra de ouro

**Se você não viu funcionar no navegador, não passou.** Toda hipótese é testada agora, ao vivo. F12 sempre aberto.

---

## 1. A história do AJAX

### Teoria

A web nasceu em 1991 **síncrona**: você clicava num link, o servidor mandava a página **inteira**, o navegador apagava tudo e desenhava de novo. Tudo piscava. Cadastro com erro? Tela branca, voltava e você redigitava tudo.

Em 1999 a Microsoft inventou o `XMLHttpRequest` (XHR) pro Outlook Web — JavaScript falando com servidor **sem recarregar**. Ninguém usou direito.

Em **2004 / 2005**, Gmail e Google Maps mostraram pra todo mundo: dá pra fazer site se comportar como aplicativo de desktop.

Em **18 de fevereiro de 2005**, Jesse James Garrett publica o artigo *"Ajax: A New Approach to Web Applications"* e batiza a técnica: **A**synchronous **J**avaScript **a**nd **X**ML.

Em **2006** John Resig lança o **jQuery**. Antes, fazer AJAX dava 25 linhas e funcionava diferente em cada navegador. Com jQuery viraram 3 linhas que funcionam em todo lugar. Em 2010, mais da metade dos sites do mundo usavam jQuery.

**Hoje (2026):** React/Vue/Angular dominam, mas **~70% dos sites ainda têm jQuery** (WordPress, Bootstrap, sistemas legados). Aprender jQuery vale porque:

1. É o jeito mais **didático** de entender AJAX.
2. Você vai pegar manutenção de sistema antigo cedo ou tarde.
3. Os conceitos (seletor, encadeamento, callback) são os mesmos de qualquer framework.

### AJAX em uma frase

> **Pedir dados ao servidor em segundo plano e atualizar só uma parte da página.**

Não é linguagem, não é produto. É **técnica**.

---

## 2. Instalação e a função `$()`

### Teoria

jQuery é **um arquivo `.js`**. Carregou, funciona.

```html
<script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
```

**Coloque sempre antes do `</body>`.** Se estiver no `<head>`, seu script tenta mexer em elementos que ainda nem existem — bug silencioso.

`$` é só **apelido** para `jQuery`. Estas duas linhas são idênticas:

```javascript
$('#titulo').text('Olá');
jQuery('#titulo').text('Olá');
```

`$()` recebe **algo** e devolve uma **coleção de elementos** com um monte de métodos.

| Você passa            | Ela faz                                       |
|-----------------------|-----------------------------------------------|
| `'#titulo'`           | seleciona o elemento de id `titulo`           |
| `'p'`                 | seleciona **todos** os parágrafos             |
| `'.aviso'`            | seleciona todos com classe `aviso`            |
| `'<div>oi</div>'`     | **cria** um div novo (na memória)             |

**Importante:** mesmo quando seleciona 1 elemento, devolve uma **coleção**. Quando você chama um método, ele age em **todos**:

```javascript
$('p').css('color', 'blue');   // pinta TODOS os <p> de azul
```

### Demo guiado — `lab21/ex01-primeiro.html`

```html
<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title>Ex01 — Primeiro jQuery</title>
</head>
<body>
    <h1 id="titulo">Página comum</h1>
    <p>Se o jQuery carregou, o título lá em cima vai mudar e ficar vermelho sozinho.</p>

    <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
    <script>
        $('#titulo').text('Funcionou!').css('color', 'crimson');
    </script>
</body>
</html>
```

**Confira no console (F12 → Console):**

```javascript
$              // ƒ (e,t){...}   ← carregou
$.fn.jquery    // "3.7.1"
```

Se aparecer `$ is not defined`, o `<script>` do jQuery não carregou.

### Atividade 1 — Cartão de visita

Crie **`lab21/ativ01-cartao.html`**.

Comece com uma página que tem só estes elementos:

```html
<h1 id="nome">Seu nome aqui</h1>
<p id="cargo">Seu cargo aqui</p>
<p id="email">seu@email.aqui</p>
```

Depois, dentro de **um único bloco** `<script>` no final do body, use jQuery para:

1. Trocar o texto do `#nome` pelo seu nome real.
2. Trocar o texto do `#cargo` por "Aluno(a) de AJAX".
3. Trocar o texto do `#email` pelo seu email.
4. Pintar o `#nome` de azul-marinho, com fonte 36px.
5. Pintar o `#email` de cinza.
6. Adicionar fundo amarelo-claro no `<body>` inteiro.

Tudo isso **sem mexer no HTML depois de criado** — só no JavaScript. O HTML fica genérico, o JS personaliza.

### Atividade 2 — Brincando no console

Abra **`ex01-primeiro.html`** no navegador. F12 → Console. Vá digitando uma linha por vez e veja a página mudar:

```javascript
$('h1').text('Hackeado!')
$('h1').css('background', 'yellow')
$('p').css('color', 'red')
$('p').css('font-size', '24px')
$('body').css('background', 'lavender')
$('body').append('<h2>Eu apareci do nada</h2>')
$('h1').fadeOut(2000)
$('h1').fadeIn(2000)
```

Anote no caderno: **o que cada uma faz**. Recarregue (F5), tente reproduzir 3 dessas mudanças **escrevendo as linhas em um script no HTML**, em vez do console.

---

## 3. Seletores

### Teoria

**Toda sintaxe CSS funciona em `$()`.** E jQuery adiciona ainda mais.

**Básicos:**

| Seletor          | Pega                                |
|------------------|-------------------------------------|
| `*`              | tudo                                |
| `p`              | todos os `<p>`                      |
| `#menu`          | id `menu`                           |
| `.aviso`         | classe `aviso`                      |
| `p.aviso`        | `<p>` que tem classe `aviso`        |
| `h1, h2, h3`     | união (qualquer um dos três)        |

**Hierárquicos:**

| Seletor    | Significa                                       |
|------------|-------------------------------------------------|
| `ul li`    | `<li>` descendente de `<ul>` (qualquer nível)   |
| `ul > li`  | `<li>` **filho direto** de `<ul>`               |
| `h1 + p`   | `<p>` imediatamente depois de `<h1>`            |

**Atributo:**

```javascript
$('input[type="text"]')      // só inputs de texto
$('a[href^="https"]')        // começa com https
$('img[src$=".png"]')        // termina com .png
$('a[href*="etec"]')         // contém "etec"
```

**Filtros jQuery (não são CSS!):**

| Filtro            | Pega                                      |
|-------------------|-------------------------------------------|
| `:first` / `:last`| primeiro / último                         |
| `:even` / `:odd`  | índice par / ímpar (zebra-striping!)      |
| `:eq(n)`          | índice `n`                                |
| `:contains("x")`  | que contém o texto "x"                    |
| `:has(sel)`       | que tem `sel` dentro                      |
| `:not(sel)`       | que **não** bate com `sel`                |

**Travessia (navegar na árvore):**

```javascript
.find(s)   .children(s)   .parent()   .parents(s)
.siblings()   .next()   .prev()
.first()   .last()   .eq(n)   .filter(s)
```

### Demo guiado — `lab21/ex02-seletores.html`

```html
<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title>Ex02 — Seletores</title>
    <style>
        body { font-family: sans-serif; max-width: 700px; margin: 30px auto; }
        table { border-collapse: collapse; margin: 10px 0; }
        td, th { border: 1px solid #999; padding: 6px 14px; }
    </style>
</head>
<body>
    <h1 id="titulo">Notas da turma</h1>

    <table id="notas">
        <tr><th>Aluno</th><th>Nota</th></tr>
        <tr><td>Ana</td>    <td>7.5</td></tr>
        <tr><td>Bruno</td>  <td>9.0</td></tr>
        <tr><td>Carla</td>  <td>5.5</td></tr>
        <tr><td>Diego</td>  <td>8.0</td></tr>
        <tr><td>Elisa</td>  <td>6.0</td></tr>
        <tr><td>Felipe</td> <td>4.0</td></tr>
    </table>

    <h2>Links</h2>
    <ul id="links">
        <li><a href="https://jquery.com">jQuery oficial</a></li>
        <li><a href="https://etec.sp.gov.br">ETEC</a></li>
        <li><a href="/local.html">página local</a></li>
        <li><a href="https://google.com">Google</a></li>
    </ul>

    <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
    <script>
        $('#notas tr:odd').css('background', '#eee');
        $('#notas tr:first').css({ background: '#cfe', fontWeight: 'bold' });
        $('a[href^="https"]').css('color', 'navy');
    </script>
</body>
</html>
```

### Atividade 3 — Cardápio temperado

Copie **`lab21/ex02-seletores.html`** para **`lab21/ativ03-cardapio.html`**. Apague a tabela e os links do HTML e coloque no lugar este cardápio:

```html
<h1>Cardápio do dia</h1>
<ul id="cardapio">
    <li class="prato">Lasanha à bolonhesa <span class="preco">R$ 32</span></li>
    <li class="prato vegetariano">Risoto de cogumelos <span class="preco">R$ 28</span></li>
    <li class="prato">Bife à parmegiana <span class="preco">R$ 38</span></li>
    <li class="prato vegetariano">Tabule <span class="preco">R$ 22</span></li>
    <li class="prato">Salmão grelhado <span class="preco">R$ 52</span></li>
</ul>
```

Apague o script de exemplo e escreva o seu, usando **só seletores e métodos do jQuery**, para:

1. Pintar todos os pratos vegetarianos com fundo verde-claro (`#cfe`).
2. Deixar todos os preços em **negrito** e azul-marinho.
3. Pintar o **primeiro** prato com fundo amarelo (use `:first`).
4. Pintar o **último** prato com fundo cinza-claro (`#eee`).
5. Adicionar, abaixo do `<ul>`, um parágrafo dizendo "Total de pratos: N" (onde N é a quantidade — use `.length` e `.after`).

### Atividade 4 — Caça aos seletores

Volte ao **`lab21/ex02-seletores.html`** (não copie, mexa nele direto — vamos quebrar e refazer). Apague o conteúdo do `<script>` e escreva **um seletor para cada uma destas tarefas**:

1. Pinte de **vermelho** apenas as notas abaixo de 7 (use `:contains` para cada uma das notas baixas).
2. Pinte de **verde** apenas as notas maiores ou iguais a 7 (mesma ideia).
3. Conte quantos links **externos** existem (que começam com `https`) e mostre o número em um `<p>` logo depois da lista.
4. Pinte o **primeiro** link da lista de azul, o **último** de roxo.
5. Sublinhe todos os links que **contêm a palavra "etec"** no `href`.

Dicas:
- `$('seletor').css(...)`
- `$('seletor').length` devolve número.
- `$('seletor').after('<p>texto</p>')` insere depois.

---

## 4. Manipulando o DOM

### Teoria

**Conteúdo:**

| Método              | Faz                                  |
|---------------------|--------------------------------------|
| `.text()` / `.text("x")`         | lê / escreve texto puro |
| `.html()` / `.html("<b>x</b>")`  | lê / escreve HTML       |
| `.val()` / `.val("x")`           | lê / escreve valor de input |

**Aparência:**

| Método                                  | Faz                          |
|-----------------------------------------|------------------------------|
| `.css("color", "red")`                  | um CSS                       |
| `.css({color:"red", fontSize:"20px"})`  | vários CSS                   |
| `.addClass("ativo")` / `.removeClass("ativo")` / `.toggleClass("ativo")` | classes |

**Estrutura:**

| Método              | Faz                                       |
|---------------------|-------------------------------------------|
| `.append(x)`        | insere `x` no **final** do elemento       |
| `.prepend(x)`       | insere `x` no **início**                  |
| `.after(x)`         | insere `x` **depois** (como irmão)        |
| `.before(x)`        | insere `x` **antes**                      |
| `.remove()`         | apaga                                     |
| `.empty()`          | esvazia o conteúdo                        |

**Efeitos:**

```javascript
.show()  .hide()
.fadeIn(ms)  .fadeOut(ms)
.slideUp(ms)  .slideDown(ms)
```

**Encadeamento** — quase todo método devolve a coleção, então dá pra encadear:

```javascript
$('#t').text('Novo').css('color', 'crimson').addClass('grande').fadeIn(1000);
```

**Eventos no jQuery** (vocês já viram `addEventListener` em JS puro — aqui é a versão jQuery):

```javascript
$('#btn').on('click', function() {
    // o que fazer quando clicar
});
```

### Demo guiado — `lab21/ex03-manipulacao.html`

```html
<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title>Ex03 — Manipulação</title>
    <style>
        body { font-family: sans-serif; max-width: 700px; margin: 30px auto; }
        button { padding: 8px 14px; margin: 4px; }
        .caixa { padding: 12px; margin: 8px 0; border: 1px solid #999; border-radius: 4px; }
        .ativo { background: #cfe; }
    </style>
</head>
<body>
    <h1 id="t">Painel de controle</h1>

    <div>
        <button id="btn-mudar">Mudar título</button>
        <button id="btn-add">Adicionar caixa</button>
        <button id="btn-ativar">Ativar todas</button>
        <button id="btn-limpar">Limpar caixas</button>
        <button id="btn-sumir">Sumir título</button>
        <button id="btn-voltar">Voltar título</button>
    </div>

    <div id="container">
        <div class="caixa">Caixa 1</div>
        <div class="caixa">Caixa 2</div>
        <div class="caixa">Caixa 3</div>
    </div>

    <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
    <script>
        $('#btn-mudar').on('click', function() {
            $('#t').text('Título mudado em ' + new Date().toLocaleTimeString())
                   .css('color', 'crimson');
        });

        var contador = 3;
        $('#btn-add').on('click', function() {
            contador++;
            $('#container').append('<div class="caixa">Caixa ' + contador + '</div>');
        });

        $('#btn-ativar').on('click', function() {
            $('.caixa').toggleClass('ativo');
        });

        $('#btn-limpar').on('click', function() {
            $('#container').empty();
            contador = 0;
        });

        $('#btn-sumir').on('click', function() { $('#t').fadeOut(800); });
        $('#btn-voltar').on('click', function() { $('#t').fadeIn(800); });
    </script>
</body>
</html>
```

### Atividade 5 — Estendendo o painel

Copie **`lab21/ex03-manipulacao.html`** para **`lab21/ativ05-painel.html`** e adicione **3 botões novos**:

1. **"Pintar caixas de azul"** — pinta todas as caixas com fundo azul-claro (`#cce`).
2. **"Apagar última caixa"** — remove **apenas a última** caixa (`.last()` + `.remove()`).
3. **"Contar caixas"** — mostra (em um `<p id="cont">`) quantas caixas tem no momento.

Para o "Contar caixas", coloque antes este parágrafo logo abaixo do `<div id="container">`:

```html
<p id="cont">Clique em "Contar" para ver.</p>
```

### Atividade 6 — Lista de tarefas (sem AJAX ainda)

Crie **`lab21/ativ06-tarefas.html`** do zero — uma lista de tarefas que vive **só no navegador** (sem servidor):

```html
<h1>Lista de tarefas</h1>
<input type="text" id="nova" placeholder="Nova tarefa...">
<button id="add">Adicionar</button>
<ul id="lista"></ul>
```

Comportamento:

1. Quando clicar em **Adicionar**, criar um `<li>` novo com o texto digitado.
2. Cada `<li>` adicionado deve ter um botão **"Apagar"** ao lado (use `.append`).
3. Quando clicar em **Apagar**, remover **aquela** `<li>`. (Dica: dentro do `.on('click', ...)`, `$(this).closest('li').remove()`.)
4. Se o campo estiver vazio quando clicar em Adicionar, **não adiciona nada**.
5. Depois de adicionar, **limpe** o campo (`$('#nova').val('')`).

Refrescante: tudo isso é só DOM. Ainda **nenhum AJAX**.

---

## 5. Primeiro AJAX

### Teoria

```javascript
$.get('url-do-servidor', function(resposta) {
    // este código roda DEPOIS, quando a resposta chega
});
```

A função no 2º argumento é o **callback**. jQuery chama ela quando a resposta volta.

**Atenção à ordem (assincronia):**

```javascript
console.log('1');
$.get('https://jsonplaceholder.typicode.com/posts/1', function(post) {
    console.log('3 — chegou:', post);
});
console.log('2');
```

No console aparece **1, 2, 3** — a linha 3 imprime depois porque foi rede. **Se você precisa do dado, só use dentro do callback.**

### A API de teste — JSONPlaceholder

| URL                                                  | Devolve            |
|------------------------------------------------------|--------------------|
| `https://jsonplaceholder.typicode.com/posts`         | 100 posts          |
| `https://jsonplaceholder.typicode.com/posts/1`       | post #1            |
| `https://jsonplaceholder.typicode.com/users`         | 10 usuários        |
| `https://jsonplaceholder.typicode.com/users/3`       | usuário #3         |

### Demo guiado — `lab21/ex04-primeiro-ajax.html`

```html
<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title>Ex04 — Primeiro AJAX</title>
    <style>
        body { font-family: sans-serif; max-width: 700px; margin: 30px auto; }
        .barra { display: flex; gap: 8px; align-items: center; margin-bottom: 14px; }
        input { padding: 6px; width: 80px; }
        button { padding: 6px 14px; }
        #resultado { border: 1px solid #ccc; padding: 14px; min-height: 100px; border-radius: 4px; }
        .carregando { color: #999; font-style: italic; }
    </style>
</head>
<body>
    <h1>Buscador de posts</h1>

    <div class="barra">
        <label>Número do post (1–100):</label>
        <input type="number" id="num" value="1" min="1" max="100">
        <button id="buscar">Buscar</button>
    </div>

    <div id="resultado">Clique em "Buscar" para começar.</div>

    <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
    <script>
        $('#buscar').on('click', function() {
            var id = $('#num').val();
            var url = 'https://jsonplaceholder.typicode.com/posts/' + id;

            $('#resultado')
                .addClass('carregando')
                .text('Carregando post #' + id + '...');

            $.get(url, function(post) {
                $('#resultado')
                    .removeClass('carregando')
                    .html(
                        '<h2>' + post.title + '</h2>' +
                        '<p>' + post.body + '</p>' +
                        '<small>Post #' + post.id + ' • Usuário ' + post.userId + '</small>'
                    );
            });
        });

        $('#buscar').trigger('click');
    </script>
</body>
</html>
```

Abra a aba **Network** (F12) e veja a requisição saindo — status 200 OK, resposta JSON.

### Atividade 7 — Buscador de usuários

Copie **`lab21/ex04-primeiro-ajax.html`** para **`lab21/ativ07-usuarios.html`** e transforme o "Buscador de posts" em **"Buscador de usuários"**.

Mudanças:

1. Troque o título e o `<label>` para indicarem usuário.
2. O input agora vai de **1 a 10** (`min="1" max="10"`).
3. A URL passa a ser `https://jsonplaceholder.typicode.com/users/ID`.
4. O resultado deve mostrar:
   - Nome do usuário (em `<h2>`)
   - Email
   - Telefone
   - Cidade (campo `address.city` do JSON)
   - Nome da empresa (`company.name`)

Dica de campos aninhados:
```javascript
usuario.address.city
usuario.company.name
```

### Atividade 8 — Botão "aleatório"

Adicione na sua **`ativ07-usuarios.html`** um botão **"Sortear usuário"** que:

1. Sorteia um número aleatório de 1 a 10.
2. Coloca esse número no input (`.val(...)`).
3. Dispara automaticamente o clique do botão "Buscar".

Dica: `Math.floor(Math.random() * 10) + 1` dá 1..10. E pra disparar clique programaticamente: `$('#buscar').trigger('click')`.

---

## Cheatsheet da Aula 1

```javascript
// SELEÇÃO
$('tag')   $('#id')   $('.classe')   $('a[href^="https"]')
$('li:first')   $('tr:odd')   $('p:contains("x")')

// MANIPULAÇÃO
.text()  .html()  .val()
.css(k, v)  .addClass(c)  .removeClass(c)  .toggleClass(c)
.append(x)  .prepend(x)  .after(x)  .before(x)
.remove()  .empty()
.fadeIn(ms)  .fadeOut(ms)

// EVENTOS
$('#btn').on('click', function() { ... });
$('#btn').trigger('click');     // dispara programaticamente

// AJAX
$.get(url, function(resposta) { ... });
```

---

## 🏆 Desafio

Crie **`lab21/desafio.html`** — um **navegador de usuários completo**.

### Requisitos visuais

- Um título "Usuários da API".
- Um cartão grande no centro mostrando: nome, email, cidade e nome da empresa.
- Três botões abaixo do cartão: **« Anterior**, **Buscar #** (com input ao lado), **Próximo »**.

### Comportamento

- Ao abrir, mostra o usuário **#1** automaticamente.
- "Anterior" e "Próximo" mudam o ID (1 a 10) e buscam.
- O input ao lado de "Buscar #" também muda o ID quando clicado.
- Enquanto a requisição estiver em curso, o cartão mostra "Carregando..." em cinza-itálico.

### Bônus

- Desabilite "Anterior" quando ID=1 e "Próximo" quando ID=10 — use `.prop('disabled', true)`.
- Use `.fadeOut` no cartão antes de carregar e `.fadeIn` depois que chegar — transição entre usuários.

---

Na próxima aula, a gente para de buscar **um** e passa a buscar **listas inteiras** — 100 posts, 10 usuários, 500 comentários — e renderizar tudo dinamicamente.
