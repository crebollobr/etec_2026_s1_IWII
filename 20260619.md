# Aula 4 — `$.ajax` completo, erros e UX

> Hoje você vai sair sabendo: usar `$.ajax({...})` com **todas** as opções, lidar com **erros de verdade** (404, 500, internet caída), aplicar `timeout`, organizar callbacks com `.done/.fail/.always`, componentizar páginas com `.load()`, e montar **vários** loadings ao mesmo tempo.
> Pasta de trabalho: `lab24/`
> API: continua a `curso.chr.eti.br/ajax/api.php`.

---

## Onde salvar seus arquivos

Sua pasta no servidor: `https://curso.chr.eti.br/SEUNOME/`. Crie `lab24/` dentro dela. **Esta aula precisa estar no servidor** — o `.load()` não funciona com `file://`.

**Versão de referência do professor:** `https://curso.chr.eti.br/professor/lab24/` — especialmente útil aqui porque o `.load()` tem que rodar via HTTP.

---

## O problema dos atalhos

`$.get`, `$.post`, `$.getJSON` são **atalhos**. Funcionam ótimo no caminho feliz. Mas você não tem como:

- Definir `timeout` (e se a rede demorar 30s?).
- Reagir a erro de forma diferente do sucesso.
- Mandar JSON puro no corpo.
- Mandar `PUT` ou `DELETE` (atalhos só fazem GET/POST).
- Adicionar headers customizados.

Pra tudo isso: **`$.ajax({...})`**.

---

## 1. A forma completa

### Teoria

```javascript
$.ajax({
    url: 'https://curso.chr.eti.br/ajax/api.php',
    method: 'GET',             // GET, POST, PUT, DELETE
    data: { aluno_id: 'ana123' },
    dataType: 'json',          // o que esperar de volta
    timeout: 5000,             // 5 segundos no máximo
    success: function(resp) { ... },
    error: function(xhr, status, erro) { ... },
    complete: function() { ... }
});
```

**Opções principais:**

| Opção         | Pra que serve                                                  |
|---------------|----------------------------------------------------------------|
| `url`         | endereço                                                        |
| `method`      | `GET`, `POST`, `PUT`, `DELETE`                                  |
| `data`        | objeto que vira `?a=1&b=2` (GET) ou corpo (POST)                |
| `dataType`    | `'json'`, `'html'`, `'text'`                                    |
| `contentType` | tipo do corpo enviado (`'application/json'` etc.)               |
| `timeout`     | milissegundos antes de desistir                                 |
| `headers`     | objeto com headers extras                                       |
| `success`     | callback de sucesso                                             |
| `error`       | callback de erro                                                |
| `complete`    | callback que sempre roda                                        |

**Ordem dos callbacks:**

1. **success** OU **error** (um dos dois)
2. **complete** (sempre)

`complete` é ótimo pra esconder loading ("acabou, com sucesso ou não").

**O parâmetro `xhr` no error:**

```javascript
xhr.status         // 404, 500, 0 (se rede caiu)…
xhr.statusText     // "Not Found", "Internal Server Error"…
xhr.responseText   // o que veio no corpo (texto)
xhr.responseJSON   // se veio JSON, já parseado
```

### Demo guiado — `lab24/ex01-ajax-completo.html`

```html
<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title>Ex01 — $.ajax</title>
    <style>
        body { font-family: sans-serif; max-width: 700px; margin: 30px auto; }
        button { padding: 8px 14px; font-size: 16px; margin: 4px 4px 4px 0; }
        #estado { margin: 14px 0; padding: 10px 14px; border-radius: 4px; font-style: italic; }
        .info { background: #eef; color: #06f; }
        .ok   { background: #efe; color: #060; font-style: normal; }
        .err  { background: #fee; color: crimson; font-style: normal; }
        .tarefa { padding: 8px; border-bottom: 1px solid #eee; }
    </style>
</head>
<body>
    <h1>$.ajax completo</h1>
    <p>Logado como: <strong id="quem"></strong></p>

    <button id="carregar">Carregar minhas tarefas</button>
    <button id="erroFake">Forçar erro 404</button>
    <button id="timeoutFake">Forçar timeout</button>

    <div id="estado"></div>
    <div id="lista"></div>

    <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
    <script>
        var ALUNO_ID = localStorage.getItem('aluno_id');
        var API = 'https://curso.chr.eti.br/ajax/api.php';
        $('#quem').text(ALUNO_ID);

        function setEstado(classe, msg) {
            $('#estado').attr('class', classe).text(msg);
        }

        $('#carregar').on('click', function() {
            setEstado('info', 'Carregando...');
            $('#lista').empty();

            $.ajax({
                url: API, method: 'GET',
                data: { aluno_id: ALUNO_ID },
                dataType: 'json', timeout: 5000,
                success: function(tarefas) {
                    setEstado('ok', 'Recebido ' + tarefas.length + ' tarefa(s).');
                    $.each(tarefas, function(i, t) {
                        $('#lista').append($('<div>').addClass('tarefa')
                            .text('#' + t.id + ' — ' + t.titulo));
                    });
                },
                error: function(xhr, status, erro) {
                    setEstado('err', 'Falhou: ' + status + ' — ' + (erro || ''));
                }
            });
        });

        $('#erroFake').on('click', function() {
            setEstado('info', 'Tentando URL inexistente...');
            $.ajax({
                url: 'https://curso.chr.eti.br/ajax/QUE-NAO-EXISTE.php',
                method: 'GET', dataType: 'json', timeout: 5000,
                error: function(xhr) {
                    setEstado('err', 'status HTTP: ' + xhr.status);
                }
            });
        });

        $('#timeoutFake').on('click', function() {
            setEstado('info', 'Pedindo com timeout absurdo de 1ms...');
            $.ajax({
                url: API, data: { aluno_id: ALUNO_ID },
                method: 'GET', dataType: 'json', timeout: 1,
                error: function(xhr, status) {
                    setEstado('err', 'Erro: ' + status);
                }
            });
        });
    </script>
</body>
</html>
```

Teste os 3 botões. Cada caso fica em **cor diferente**.

### Atividade 1 — Botão "Ping API"

Crie **`lab24/ativ01-ping.html`**. Uma tela simples com:

- Um botão grande **"PING"** no centro.
- Embaixo, um indicador: "(aguardando)" / "✓ ON-LINE (Nms)" / "✗ OFF-LINE".

Comportamento:

1. Ao clicar, mede `var inicio = Date.now()`.
2. Faz `$.ajax` para `https://curso.chr.eti.br/ajax/api.php?aluno_id=ping_test` com `timeout: 3000`.
3. No `complete`, calcula `Date.now() - inicio` e mostra.
4. Verde se ok, vermelho se falhou.

### Atividade 2 — Cardápio de erros

Crie **`lab24/ativ02-erros.html`** com **4 botões**, cada um forçando um cenário de erro diferente:

1. **"Sucesso normal"** — chamada válida (`api.php?aluno_id=x`).
2. **"Erro 404"** — URL inexistente.
3. **"Timeout"** — timeout: 1ms.
4. **"Domínio quebrado"** — URL com domínio inválido: `https://nao-existe.aiii/x`.

Para cada um, mostre numa caixa:
- `xhr.status` (o número HTTP)
- `status` (string: "success", "error", "timeout"...)
- O texto da resposta (se houver) — `xhr.responseText.substring(0, 100)` (cortado em 100 chars).

---

## 2. `.done`, `.fail`, `.always`

### Teoria

`$.ajax` devolve um **objeto promise**. Em vez de passar `success/error/complete` no objeto, dá pra **encadear**:

```javascript
$.ajax({ url: API, dataType: 'json' })
    .done(function(resp) { /* sucesso */ })
    .fail(function(xhr) { /* erro */ })
    .always(function() { /* sempre */ });
```

Funciona com `$.get`, `$.post`, `$.getJSON` também:

```javascript
$.getJSON(url)
    .done(function(dados) { ... })
    .fail(function(xhr) { ... });
```

**Vantagens:**

- Lê de cima pra baixo, sem aninhar.
- Pode adicionar mais `.done` depois (em outros pontos do código).

### Demo guiado — `lab24/ex02-promises.html`

```html
<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title>Ex02 — .done .fail</title>
    <style>
        body { font-family: sans-serif; max-width: 700px; margin: 30px auto; }
        button { padding: 8px 14px; }
        #log { font-family: monospace; background: #111; color: #0f0;
               padding: 12px; min-height: 100px; border-radius: 4px; }
        #log div.err { color: #f66; }
    </style>
</head>
<body>
    <h1>.done / .fail / .always</h1>

    <button id="ok">Tentar URL boa</button>
    <button id="ruim">Tentar URL ruim</button>

    <h3>Log:</h3>
    <div id="log"></div>

    <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
    <script>
        var ALUNO_ID = localStorage.getItem('aluno_id');
        var API = 'https://curso.chr.eti.br/ajax/api.php';

        function log(msg, erro) {
            var $linha = $('<div>').text('[' + new Date().toLocaleTimeString() + '] ' + msg);
            if (erro) $linha.addClass('err');
            $('#log').append($linha);
        }

        $('#ok').on('click', function() {
            log('iniciando GET...');
            $.getJSON(API + '?aluno_id=' + ALUNO_ID)
                .done(function(tarefas) { log('✓ ' + tarefas.length + ' tarefa(s)'); })
                .fail(function(xhr)     { log('✗ ' + xhr.status, true); })
                .always(function()      { log('— fim —'); });
        });

        $('#ruim').on('click', function() {
            log('tentando URL inexistente...');
            $.getJSON('https://curso.chr.eti.br/ajax/NAO-EXISTE.php')
                .done(function()    { log('era pra dar errado'); })
                .fail(function(xhr) { log('✗ ' + xhr.status, true); })
                .always(function()  { log('— fim —'); });
        });
    </script>
</body>
</html>
```

### Atividade 3 — Histórico de tentativas

Copie **`lab24/ex02-promises.html`** para **`lab24/ativ03-historico.html`** e adicione:

1. Um contador no topo: "X tentativas — Y sucessos — Z falhas".
2. Cada clique incrementa "tentativas".
3. `done` incrementa "sucessos", `fail` incrementa "falhas".
4. Use `.always` pra atualizar a UI sempre.

### Atividade 4 — Botão "Tentar de novo"

Crie **`lab24/ativ04-retry.html`**. Tela com:

- Um botão "Carregar tarefas".
- Uma área de resultado.
- Inicialmente o botão é apertado **automaticamente**.

Comportamento:

1. No `fail`, mostra o erro em vermelho + um botão **"Tentar de novo"** que **recarrega**.
2. No `done`, esconde o botão de retry.

Estrutura sugerida:

```javascript
function carregar() {
    $('#tentar').hide();
    $('#estado').text('Carregando...');

    $.getJSON(URL)
        .done(function(t) { /* mostra */ })
        .fail(function() {
            $('#estado').text('Falhou.');
            $('#tentar').show();
        });
}

$('#tentar').on('click', carregar);
carregar();
```

---

## 3. Várias requisições em paralelo

### Teoria

Quando você precisa de dados de **dois lugares** ao mesmo tempo, **não encadeie** (lento). Lance os dois **juntos**.

```javascript
$.getJSON('url-a').done(renderA).fail(mostraErroA);
$.getJSON('url-b').done(renderB).fail(mostraErroB);
```

Isso é melhor que encadear porque cada um falha/sucede **independente** — uma falha não derruba a outra.

### Demo guiado — `lab24/ex03-dashboard.html`

```html
<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title>Ex03 — Dashboard</title>
    <style>
        body { font-family: sans-serif; max-width: 1100px; margin: 30px auto; }
        #grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 16px; }
        .widget { background: #fff; border: 1px solid #ddd; border-radius: 6px; padding: 14px; min-height: 200px; }
        .widget h2 { margin: 0 0 10px; font-size: 18px; }
        .estado { font-style: italic; padding: 8px 0; }
        .info { color: #06f; }
        .err  { color: crimson; font-style: normal; }
        ul { margin: 0; padding: 0 0 0 18px; }
        li { padding: 3px 0; }
    </style>
</head>
<body>
    <h1>Painel de controle</h1>

    <div id="grid">
        <div class="widget">
            <h2>Minhas tarefas</h2>
            <div class="estado info" id="estado-t">Carregando...</div>
            <ul id="lista-t"></ul>
        </div>

        <div class="widget">
            <h2>Usuários (placeholder)</h2>
            <div class="estado info" id="estado-u">Carregando...</div>
            <ul id="lista-u"></ul>
        </div>

        <div class="widget">
            <h2>Posts (placeholder)</h2>
            <div class="estado info" id="estado-p">Carregando...</div>
            <ul id="lista-p"></ul>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
    <script>
        var ALUNO_ID = localStorage.getItem('aluno_id');
        var API = 'https://curso.chr.eti.br/ajax/api.php';

        $.ajax({ url: API, data: { aluno_id: ALUNO_ID }, dataType: 'json', timeout: 5000 })
            .done(function(tarefas) {
                $('#estado-t').removeClass('info').text(tarefas.length + ' tarefa(s)');
                $.each(tarefas, function(i, t) {
                    $('#lista-t').append($('<li>').text(t.titulo));
                });
            })
            .fail(function(xhr) {
                $('#estado-t').removeClass('info').addClass('err').text('Erro: ' + xhr.status);
            });

        $.ajax({ url: 'https://jsonplaceholder.typicode.com/users', dataType: 'json', timeout: 5000 })
            .done(function(usuarios) {
                $('#estado-u').removeClass('info').text(usuarios.length + ' usuários');
                $.each(usuarios, function(i, u) {
                    $('#lista-u').append($('<li>').text(u.name));
                });
            })
            .fail(function(xhr) {
                $('#estado-u').removeClass('info').addClass('err').text('Erro: ' + xhr.status);
            });

        $.ajax({ url: 'https://jsonplaceholder.typicode.com/posts?_limit=5', dataType: 'json', timeout: 5000 })
            .done(function(posts) {
                $('#estado-p').removeClass('info').text(posts.length + ' posts');
                $.each(posts, function(i, p) {
                    $('#lista-p').append($('<li>').text(p.title));
                });
            })
            .fail(function(xhr) {
                $('#estado-p').removeClass('info').addClass('err').text('Erro: ' + xhr.status);
            });
    </script>
</body>
</html>
```

Os 3 widgets dizem "Carregando..." e vão sendo preenchidos. Se um falhar, ele fica em **vermelho**, **os outros continuam**.

### Atividade 5 — Dashboard de notícias

Crie **`lab24/ativ05-noticias.html`** com **4 widgets** que carregam endpoints diferentes:

| Widget         | URL                                                       |
|----------------|-----------------------------------------------------------|
| Usuários       | `https://jsonplaceholder.typicode.com/users`              |
| Posts          | `https://jsonplaceholder.typicode.com/posts?_limit=5`     |
| Comentários    | `https://jsonplaceholder.typicode.com/comments?_limit=5`  |
| Fotos          | `https://jsonplaceholder.typicode.com/photos?_limit=4`    |

Cada widget mostra o conteúdo de um jeito apropriado (lista, lista, lista, mini-grade de imagens). Estilo livre.

### Atividade 6 — Dashboard com refresh

Copie **`lab24/ex03-dashboard.html`** para **`lab24/ativ06-refresh.html`** e adicione:

1. Botão grande **"🔄 Atualizar tudo"** acima do grid.
2. Ao clicar, limpa os 3 widgets, marca "Carregando..." e refaz as 3 chamadas.
3. Mostre **"Última atualização: HH:MM:SS"** abaixo do botão — atualizado a cada refresh.

Organize as 3 chamadas em uma função `function carregarTudo() { ... }`. O botão chama essa função. Ao abrir a página também chama.

---

## 4. `.load()` — componentizar HTML

### Teoria

`.load(url)` baixa **HTML** de uma URL e enfia direto dentro de um elemento. Útil pra "componentizar" — mesmo header, menu e rodapé em várias páginas.

```javascript
$('#topo').load('header.html');
$('#rodape').load('footer.html');
```

> **Atenção:** `.load()` chama o servidor. Tem que abrir via **http** (não `file://`). Como você já tem sua pasta em `curso.chr.eti.br/SEUNOME/`, abra por essa URL.

Você pode até pegar **só um pedaço**:

```javascript
$('#topo').load('header.html #nav');   // só o elemento #nav do arquivo
```

### Demo guiado

Crie 3 arquivos:

**`lab24/header.html`:**

```html
<nav style="background:#222; color:#fff; padding:12px 20px;">
    <strong>ETEC AJAX 2026</strong> &nbsp;|&nbsp; Home &nbsp;|&nbsp; Aulas &nbsp;|&nbsp; Contato
</nav>
```

**`lab24/footer.html`:**

```html
<footer style="background:#eee; padding:10px 20px; margin-top:30px; color:#666; font-size:13px;">
    © 2026 — Curso AJAX da ETEC
</footer>
```

**`lab24/ex04-componentes.html`:**

```html
<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title>Ex04 — .load()</title>
    <style>
        body { margin: 0; font-family: sans-serif; }
        main { max-width: 800px; margin: 0 auto; padding: 20px; }
    </style>
</head>
<body>
    <div id="topo"></div>

    <main>
        <h1>Página com header/footer carregados</h1>
        <p>O cabeçalho lá em cima e o rodapé lá embaixo vieram de fora.</p>
    </main>

    <div id="rodape"></div>

    <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
    <script>
        $('#topo').load('header.html');
        $('#rodape').load('footer.html');
    </script>
</body>
</html>
```

Abra a página via sua URL pública (`https://curso.chr.eti.br/SEUNOME/lab24/ex04-componentes.html`). Você verá header preto em cima, conteúdo no meio, footer cinza embaixo.

**Mude o `header.html`** (adicione um link), salve, F5. **Todas** as páginas que usam o header mudam juntas.

### Atividade 7 — Site de 3 páginas componentizado

Crie no `lab24/`:

- `menu.html` — um `<nav>` com 3 links: `pagina1.html`, `pagina2.html`, `pagina3.html`.
- `rodape.html` — um `<footer>` com seu nome e a data.
- `pagina1.html`, `pagina2.html`, `pagina3.html` — cada uma com conteúdo diferente, **mas todas carregam o mesmo `menu.html` no topo e o mesmo `rodape.html` embaixo** via `.load()`.

Comportamento:

1. Clicando no link do menu, navega entre as páginas normalmente.
2. O menu e o rodapé continuam idênticos.
3. Verifique que mexer em `menu.html` muda nas 3 páginas.

### Atividade 8 — Carregando só um pedaço

Crie **`lab24/ativ08-pedaco.html`**. Use `.load()` com seletor para puxar **só uma parte** de outro arquivo.

1. Crie `lab24/biblioteca.html` contendo várias seções:

```html
<section id="bio">
    <h2>Sobre mim</h2>
    <p>Texto longo aqui...</p>
</section>

<section id="contato">
    <h2>Contato</h2>
    <p>Email, telefone, etc.</p>
</section>

<section id="hobbies">
    <h2>Hobbies</h2>
    <p>Música, jogos, leitura.</p>
</section>
```

2. Em `ativ08-pedaco.html`, faça 3 botões: **"Sobre"**, **"Contato"**, **"Hobbies"**. Cada clique carrega **só aquela seção** dentro de um `<div id="conteudo">`:

```javascript
$('#btn-sobre').on('click', function() {
    $('#conteudo').load('biblioteca.html #bio');
});
```

---

## Cheatsheet da Aula 4

```javascript
// $.ajax completo
$.ajax({
    url: ..., method: 'GET', data: {...},
    dataType: 'json', timeout: 5000,
    headers: { 'X-Custom': 'valor' }
})
.done(function(resp){ ... })
.fail(function(xhr){ console.log(xhr.status); })
.always(function(){ ... });

// xhr no error
xhr.status          // 200, 404, 500, 0
xhr.statusText
xhr.responseText
xhr.responseJSON

// Várias em paralelo
$.getJSON(u1).done(renderA).fail(mostraErroA);
$.getJSON(u2).done(renderB).fail(mostraErroB);

// Componentizar
$('#topo').load('header.html');       // página inteira
$('#topo').load('header.html #nav');  // só parte
```

---

## 🏆 Desafio

Crie **`lab24/desafio.html`** — **monitor de saúde da API**.

### Requisitos

Página com **3 botões grandes**, um abaixo do outro, cada um representando um teste:

1. **Testar API do curso** — `GET api.php?aluno_id=...`
2. **Testar JSONPlaceholder** — `GET https://jsonplaceholder.typicode.com/posts/1`
3. **Testar URL inexistente** — `GET https://curso.chr.eti.br/ajax/NAO-EXISTE.php`

Para cada teste, ao clicar:

- Mostre "⏳ Testando..." (amarelo) ao lado do botão.
- Quando voltar: **status HTTP** + **tempo em ms** + **✓ ou ✗**.
- Verde se sucesso, vermelho se erro.

### Dica: medir tempo

```javascript
var inicio = Date.now();
$.ajax({...}).always(function() {
    var ms = Date.now() - inicio;
});
```

### Bônus

- Botão "**Testar todos**" que dispara os 3 ao mesmo tempo (em paralelo).
- Quando todos voltarem, mostra "✓ Bateria completa" embaixo.

### Visual sugerido

```
+----------------------------------------------+
| Monitor de saúde                             |
+----------------------------------------------+
| [ Testar API do curso     ]  ⏳ Testando...   |
| [ Testar JSONPlaceholder  ]  ✓ 200 — 142ms   |
| [ Testar URL inexistente  ]  ✗ 404 — 89ms    |
|                                              |
| [ Testar todos ]                             |
+----------------------------------------------+
```

---

Na próxima aula vamos completar o ciclo: **PUT** (editar), **DELETE** (apagar), e montar o **projeto final** — um gerenciador de tarefas com CRUD completo.
